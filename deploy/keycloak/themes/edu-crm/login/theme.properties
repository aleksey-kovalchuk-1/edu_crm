# Minimal override of the built-in theme (see docs/decisions.md D-156): everything except the one
# message key below (the custom .ru-only email validator's error message, which Keycloak's own
# translation bundle does not have because the validator is ours) comes from the parent theme.
parent=keycloak.v2
import=common/keycloak
