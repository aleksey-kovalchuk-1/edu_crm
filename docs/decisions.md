# Decisions and Assumptions

Sources of authority, highest first: owner decisions → `docs/specification.md` → technical decisions below. Earlier AI-generated plans are not requirements.

Status values: `OWNER` (explicit owner decision), `ASSUMED` (default the owner may override), `PROPOSAL` (needs owner approval before implementation).

## Owner decisions

| ID | Decision | Status |
|---|---|---|
| D-001 | Stack: React, TypeScript, FastAPI, PostgreSQL, Docker | OWNER |
| D-002 | Authentication through Keycloak; no custom password authentication | OWNER |
| D-003 | No external university portal at this stage | OWNER |
| D-004 | Do not remove existing modules (tasks, Superset) because earlier reports suggested it | OWNER |
| D-005 | Work on an `ai/` branch; push the branch and draft PRs allowed; never merge to `main` | OWNER |
| D-006 | Never delete database volumes; use compatible migrations and backups | OWNER |
| D-007 | Synthetic data only; no personal data or secrets in source control, screenshots, logs, artifacts | OWNER |
| D-008 | Mock LMS and website contracts until real ones are provided, labelled as mocks | OWNER |
| D-009 | Run until 2026-09-16 08:30 MSK within the Pro plan limits; when a command needs approval, continue with other work | OWNER |
| D-010 | Major product changes, architecture replacements, new external services, destructive changes require owner approval | OWNER |

## Technical decisions

| ID | Decision | Rationale | Status |
|---|---|---|---|
| D-101 | Roles: `crm-user` (CAM), `crm-supervisor`, `crm-admin` as Keycloak realm roles | Matches the specification's three roles | ASSUMED |
| D-102 | Keycloak holds identity and roles; the CRM database holds data-visibility scopes managed by the Administrator in the CRM | Scope checks stay inside SQL queries | ASSUMED |
| D-103 | Visibility: User sees and edits assigned universities; Supervisor sees all and reassigns responsible persons; Administrator sees all and manages access | Least-privilege default | ASSUMED |
| D-104 | Login is server-side (backend-for-frontend): confidential OIDC client, sessions in PostgreSQL, httpOnly cookie, CSRF token | Tokens never reach browser JavaScript | ASSUMED |
| D-105 | "Cache of user actions" = audit trail of who did what and when, shown as recent actions | Most useful reading of an ambiguous requirement | ASSUMED |
| D-106 | PostgreSQL is the only database, including tests (template database cloned per test) | Migrations must be tested on the production engine | ASSUMED |
| D-107 | Schema changes only through Alembic; existing databases are stamped, never recreated | D-006 | ASSUMED |
| D-108 | Russian ordering through per-column ICU collation (`ru-RU-x-icu`), not by re-initialising the cluster | Verified available in `postgres:16-alpine`; respects D-006 | ASSUMED |
| D-109 | Report jobs use a PostgreSQL-backed queue and a worker container; no Redis | Meets 10 parallel reports without a new service | ASSUMED |
| D-110 | Attachments stored on a Docker volume behind a storage interface; content-based type checks and size limits | No new service; MinIO and antivirus scanning are proposals | ASSUMED |
| D-111 | Legacy `.xls` export by converting xlsx with LibreOffice in the worker image | The specification requires `.xls`; Python `.xls` writers are unmaintained | ASSUMED |
| D-112 | Russian user interface and documentation; English code, comments, commit messages | Target users are Russian-speaking; code stays conventional | ASSUMED |
| D-113 | Python 3.12 in Docker and CI is authoritative; local 3.14 is for convenience | Matches the production image | ASSUMED |
| D-114 | Milestone tags use `ai-mN-<name>-YYYYMMDD` and are never moved | Unique, recoverable versions | ASSUMED |
| D-115 | The lead agent alone owns migrations, the API contract, and Compose/deployment files; subagents get non-overlapping files | Avoids conflicting edits | ASSUMED |
| D-116 | The A1 spec draft (custom session authentication) is superseded by D-002; kept for history | Owner decision overrides it | ASSUMED |
| D-117 | Keycloak `quay.io/keycloak/keycloak:26.7.3`; realm `edu-crm` imported from `deploy/keycloak/realm-edu-crm.json`; client secret and demo passwords substituted from environment variables (`${VAR}`), required in Compose with `${VAR:?}` because an unset variable is imported as literal text | Verified in a throwaway container on 2026-09-15 (see night report) | ASSUMED |
| D-118 | The backend keeps only `crm-*` values from the token `roles` claim; built-in roles (`default-roles-edu-crm`, `offline_access`, `uma_authorization`) are ignored | Tokens carry built-in roles alongside CRM roles | ASSUMED |
| D-120 | Every API error response is `{"code", "message", "details"}` with codes from `backend/app/errors.py`, documented in `docs/api/errors.md`; the frontend shows `message` and the `code` | Specification requires error codes; one shape keeps clients simple | ASSUMED |
| D-121 | Frontend: React Router for pages, TanStack Query for server state with targeted cache invalidation, Vitest + Testing Library + ESLint for checks | Multiple screens and the no-reload / 1-second requirements; current single file refetches everything after each change | ASSUMED |
| D-122 | Keycloak is published through nginx at `/auth` (`KC_HTTP_RELATIVE_PATH=/auth`, `KC_HOSTNAME=http://localhost:8080/auth`, `KC_PROXY_HEADERS=xforwarded`, `KC_HOSTNAME_BACKCHANNEL_DYNAMIC=true`); the API reaches it at `http://keycloak:8080/auth` | One browser origin and port; stable token issuer | ASSUMED |
| D-123 | Keycloak runs in production mode (`start --import-realm`) with its own PostgreSQL database `keycloak` and role on the existing server, created idempotently by a one-shot init service | Supported, persistent mode; the data volume is not re-initialised | ASSUMED |
| D-124 | OIDC authorization-code flow with PKCE implemented with `httpx` and `PyJWT[crypto]` (JWKS verification); login state stored in PostgreSQL | Few dependencies; any API worker can complete the callback | ASSUMED |
| D-125 | Sessions keep the Keycloak refresh token encrypted (Fernet key from environment) and revalidate with Keycloak at most every 120 s; a failed refresh revokes the CRM session | Disabling a user or ending the Keycloak session stops CRM access within minutes | ASSUMED |
| D-126 | A `users` table mirrors Keycloak identity (`sub`, email, name, cached `crm-*` roles) for assignments and audit; Keycloak stays the source of roles | CRM data (responsible persons, audit) needs stable local user references | ASSUMED |
| D-127 | Russian ordering is implemented as ICU collation `ru-RU-x-icu` on each user-visible text column (migration `0002`) | Implements D-108 on existing databases | ASSUMED |
| D-128 | Universities are catalog data: only `crm-supervisor` and `crm-admin` may create them; all three roles read data, create launches, change stages and tasks until data-visibility scopes are implemented | Catalogs are maintained by heads and administrators; managers work on interactions | ASSUMED |
| D-129 | Local secrets (Keycloak admin, Keycloak database role, client secret, demo passwords, session encryption key) are generated by `scripts/generate-dev-secrets.sh` into gitignored `deploy/local/*.env` files loaded through Compose `env_file`; `.env` holds only non-secret defaults | Nothing secret is committed; setup works without editing `.env` | ASSUMED |
| D-130 | Agents never type passwords into the Keycloak login form; the login flow is verified by tests against a fake identity provider, by HTTP checks of the real redirect, page and discovery, and the final browser sign-in is left to the owner | Safety rule for credentials | OWNER |
| D-131 | Audit trail (D-105): `audit_events` stores actor, action, entity, Russian summary, JSON payload and IP; written in the same transaction as the change; managers see their own recent actions, heads and administrators see everyone's | Least privilege for personal activity data | ASSUMED |
| D-132 | Swagger UI and the OpenAPI schema (`/api/docs`, `/api/openapi.json`) stay public; they describe the API but expose no data, and every data endpoint requires a session | The specification requires all methods to be described in Swagger UI | ASSUMED |
| D-133 | A missing or invalid CSRF token or untrusted `Origin` returns `403 CSRF_INVALID`, distinct from `403 FORBIDDEN` | The interface can refresh the token instead of reporting a permission problem (frontend review suggestion) | ASSUMED |
| D-134 | The login callback is bound to the browser that started it with a short-lived `edu_crm_login` cookie whose hash is stored in `login_states` | Prevents login CSRF (security review finding 3) | ASSUMED |
| D-135 | Keycloak outages (network errors, 5xx, signing keys unavailable) return `503` without revoking sessions; only explicit rejections revoke. Logout ends the Keycloak session server-side before revoking the CRM session | Security review findings 2 and 4 | ASSUMED |
| D-136 | Catalog entities and uniqueness: `it_directions` (name), `it_products` (vendor, name) with many-to-many directions, `university_contacts` (university, full name), `university_managers` (university, user), `contracts` (contract number) with many-to-many university contacts; see `docs/design/catalogs.md` | Mirrors the specification's catalogs and import fields with stable keys for idempotent import | ASSUMED |
| D-137 | "Responsible persons from the School" are CRM users assigned to universities; "responsible persons from the University" are `university_contacts` records (personal data) | Separates staff accountability (roles, scopes) from external contacts | ASSUMED |
| D-138 | Licence validity defaults to signing date + 1 year when not given, is stored, may be edited, and may not precede the signing date | Specification: validity period of one year | ASSUMED |
| D-139 | Transfer status values: `not_started`, `in_progress`, `transferred`, `cancelled` with Russian labels | Specification names the field but not its values | ASSUMED |
| D-140 | Import matches managers to CRM users by normalised full name; unmatched or ambiguous names are kept as text and reported; no accounts are created automatically | Avoids silently creating identities outside Keycloak | ASSUMED |
| D-141 | Data scopes: a manager (`crm-user`) sees and edits only universities where they are an assigned manager, enforced in SQL; heads and administrators see all | Implements D-103 | ASSUMED |
| D-142 | Catalog import is a three-step flow (upload with suggested mapping → dry-run check → apply) over parsed rows stored in `catalog_imports`; the original file is not stored | Users can correct the mapping and see effects before data changes; nothing binary is kept | ASSUMED |
| D-143 | The agreed field mapping is expressed as header synonyms per CRM field, overridable per upload; saved mapping profiles are deferred | The owner has not provided a sample file; synonyms cover Russian and English headers | ASSUMED |
| D-144 | Import applies all valid rows in one transaction, skips invalid rows with reasons, and records one `import.apply` audit event with counts | Predictable partial imports without half-written rows; audit stays readable | ASSUMED |
| D-145 | Import limits: 10 MB file, 5000 data rows, 100 MB total uncompressed xlsx parts, type checked by file signature; `.xlsx` via openpyxl (read-only, cached values), `.xls` via xlrd | Protects the API from oversized and zip-bomb uploads | ASSUMED |
| D-146 | Import creates missing universities, IT products, IT directions and university contacts, and updates contracts matched by number; managers are matched by full name only (D-140) | Matches "catalogs updated by uploading files" in the specification | ASSUMED |
| D-147 | Yearly statistics (applications, students, streams) on the dashboard are company-wide aggregates visible to every role; other dashboard numbers follow the user's data scope and count only active universities | Aggregates without personal data, used for program ranking; answers catalog review finding 3 | ASSUMED |
| D-148 | A manager may set only themselves (or nobody) as a contract's manager; heads and administrators may choose any active user. Inactive universities, products and contacts cannot be newly linked, but existing links stay editable | Prevents misassigned contracts and user-id probing (catalog review findings 2 and 5) | ASSUMED |
| D-149 | Workflows are templates with ordered, renamable statuses; launches reference a status by id, so renaming or reordering never rewrites history; `launches.stage` is kept in sync with the status position for existing endpoints | Specification: adjust status names and create/modify workflows | ASSUMED |
| D-150 | The default template «Типовое взаимодействие с вузом» has the 13 current statuses covering specification points 1–13; point 14 (control over each stage) is provided by deadlines, overdue tracking, history and reports rather than a status | Point 14 describes monitoring, not a step | ASSUMED |
| D-151 | Any active status of the same template may be chosen, including going back for revision; restricted transitions wait for the customer's regulations | The specification allows switching between statuses and no regulations were provided | ASSUMED |
| D-152 | Status changes store from/to status, optional comment (≤2000), author and time; existing `stage_events` are migrated into `status_changes` without author | One history for old and new data | ASSUMED |
| D-153 | Attachments: png, jpeg, pdf, zip, gzip, rar, doc, docx, xls, xlsx checked by content signature and extension; ≤20 MB per file, ≤5 per status change; stored on a Docker volume under random keys with SHA-256 in the database; downloaded as attachments with `nosniff` | Specification file formats; safe storage without new services (D-110) | ASSUMED |
| D-154 | Heads and administrators edit workflow templates; deactivated statuses stay in history but cannot be chosen; a status currently used by launches cannot be deactivated; the default template cannot be deactivated | Keeps existing interactions consistent | ASSUMED |
| D-155 | Phone verification is a CRM-owned feature, not a Keycloak/OIDC change: `users.phone`/`phone_verified_at` live in the CRM's own database (D-102/D-126 already put profile-adjacent facts there), not as a Keycloak custom attribute, and no Keycloak authenticator flow is touched — D-002 (Keycloak is the sole identity provider) stays intact. A true SMS second factor *inside login itself* would need a custom Keycloak Authenticator SPI (Java, no toolchain in this project) and is explicitly out of scope; owner declined it in favour of this simpler approach | Owner decision 2026-09-16: "create phone verification as a crm-owned feature", explicitly not SMS-in-registration | OWNER |
| D-156 | The code-send step runs synchronously in the request handler (one short outbound HTTP call, timeout-bounded), not through a background job queue: unlike bulk file-ingestion work, a single SMS send is comparable in cost to the OIDC token-exchange calls this codebase already makes synchronously in `auth.py`. Keeps this feature independent of the (separate, unmerged) `ai/file-ingestion` branch's job-queue infrastructure | Simplicity; avoids depending on unmerged infrastructure from a different, independent feature branch | ASSUMED |
| D-157 | No real SMS gateway is integrated. `app/sms.py` defaults to a logging-only sender (the code is written to the API container's log, analogous to how Mailpit lets local dev "see" email) unless `SMS_PROVIDER_URL`/`SMS_PROVIDER_API_KEY` are set in `deploy/local/api.env` (gitignored, never committed), matching the established SMTP/reCAPTCHA pattern. A generic configurable HTTP POST client is provided since the real (presumably Rostelecom-operated) gateway's exact API contract is not available in this environment | No real provider credentials or API spec available to integrate against or test with | ASSUMED |
| D-119 | Constraint names follow PostgreSQL defaults (`<table>_pkey`, `<table>_<column>_fkey`, `<table>_<column>_key`) through the SQLAlchemy naming convention | Databases built by `create_all` before migrations keep identical names, so the baseline can be stamped | ASSUMED |
| D-158 | Tasks workspace redesign (docs/design/tasks.md; T-100s) built on `ai/tasks-workspace`, forked from `ai/phone-verification` — not from `ai/auth-registration`/`ai/registration-polish`, a separately-diverged history (found while working on that branch in an earlier session) that has self-registration/CAPTCHA but was never reconciled with this one. The rename to "UniCrm" made on that other lineage does not carry over here | Reuse the actually-existing catalogs/workflows/audit modules the task spec assumes, without merging two unreconciled histories in the same change | ASSUMED |
| D-159 | Migration 0010 extends the placeholder `tasks` table (`launch_id`, `title`, `owner`, `deadline`, `done`) in place rather than replacing it: every existing row survives, `status` backfills from `done`, `owner` (free text) is matched to `creator_id` only when it matches exactly one active user's `full_name` case/whitespace-insensitively, otherwise `creator_id` stays NULL and the original `owner` text is left untouched as the administrator-review signal (no separate flag column). `owner`/`done` are not dropped and get a server default (`''`/`false`) so a new task can be created without them, but no new code reads or writes them — the legacy `GET/PATCH /api/v1/tasks` endpoints keep working against them unchanged until a later slice replaces those endpoints | Spec requirement: no data loss, no invented certainty about an ambiguous owner match | ASSUMED |
| D-160 | Concurrent edits to the same task are caught with SQLAlchemy's native `version_id_col` optimistic locking (`tasks.version`, starts at 1) rather than a hand-rolled `If-Match`/timestamp check: a stale write raises `StaleDataError`, which the route layer (next slice) translates to `409 CONFLICT` the same way `IntegrityError` already is in `catalog_routes.py`/`workflow_routes.py` | Least code for a well-supported ORM feature that fits the existing commit-and-catch-conflict pattern already used everywhere else | ASSUMED |
| D-161 | Task permissions centralised in `app/task_policy.py` (`can(user, action, task=None)`, `visible_tasks_query(user)`) instead of role checks scattered per route, extending the existing `sees_all()`/`managed_university_ids()` from `catalog_routes.py` with a task-membership dimension (creator / assignee / participant / observer) that university-scoped role checks alone don't cover. The action set (`TaskAction`) matches what a future Settings → "Users and roles" matrix would need to configure, so that matrix can later replace `can()`'s body with a data-driven lookup without touching call sites — no such matrix UI is built now | Spec requirement: centralize authorization, design for (not build) a future permission matrix | ASSUMED |
| D-162 | Slice 2 (`task_routes.py`) fully replaces, rather than adds alongside, the placeholder `GET/PATCH /api/v1/tasks(/{id})` in `main.py`: the two contracts (bare array + `done` boolean vs. paginated `{items,total,limit,offset}` + stable `status` codes) can't coexist at the same route, and keeping both would mean two different shapes answering the same path. `TaskInput`/`schemas.TaskInput` removed with it; the legacy audit/API tests that exercised the old shape were rewritten against the new one, not deleted, so the same behaviors (no-op = no audit event, before/after payload, 404 on a bad id) stay covered | The old contract was explicitly a placeholder ("replace the current basic Tasks page"); this slice's own "List view" work is the replacement moment the design doc already flagged | ASSUMED |
| D-163 | List pagination uses `limit`/`offset` (matching the existing `GET /contracts` convention and the frontend's shared `Page<T>` type) — the first draft of this endpoint used `page`/`page_size` before this inconsistency was caught in review and fixed before merging | Consistency with the one other paginated list endpoint in the codebase, reusing frontend types instead of inventing a second pagination shape | ASSUMED |
| D-164 | Added `GET /tasks/assignable-users` (any signed-in CRM user; returns only `{id, full_name}`) rather than widening the existing `GET /users` (supervisor/admin only, includes email and roles) for the task-creation assignee picker: the spec requires every role to be able to assign multiple people to a task, but `GET /users` was deliberately scoped narrower as an admin-level directory. A new, minimally-scoped endpoint preserves that existing boundary instead of quietly loosening it | Multiple assignees must work for every role (spec); avoid broadening a more sensitive existing endpoint's access as a side effect | ASSUMED |
| D-165 | The Tasks workspace's "Мой план"/Deadline/full-filter views, checklists, subtasks, status transitions, comments, and plan templates are not built yet — this slice ships List view (search, scope tabs, sort, pagination, URL state), standalone/linked creation with multiple assignees and a nullable deadline, and the `/tasks/:id` deep link only, per the spec's own slice ordering. The sidebar task-count badge shows the total in "Мои задачи" scope (not filtered to open-only) until slice 4 adds real counters | Keep every slice runnable and match the spec's explicit "Implementation order" rather than partially building later slices' features | ASSUMED |
| D-166 | File-upload validation/storage (content-signature check, streaming to the attachments volume) moved out of `workflow_routes.py` into a shared `app/uploads.py`, used by both launch status-change attachments and the new task comment attachments — `store_upload()` now returns plain fields instead of building an `Attachment` ORM row itself, so it stays model-agnostic between `Attachment` and the new `TaskAttachment` | Slice 3 needs the same upload logic for task comments; duplicating ~70 lines instead of sharing them would only skew from the very first task line ("Keep the application a modular monolith") | ASSUMED |
| D-167 | Status transitions are a fixed `(from, to) -> 'assignee' \| 'manager'` table in `task_routes.py`, not a per-task configurable workflow like `WorkflowTemplate`/`WorkflowStatus` (interactions): the spec gives one fixed state machine (`new → in_progress → awaiting_review → completed`, plus `deferred`/`cancelled`) for all tasks, not a per-template one, so the extra configurability those tables provide isn't needed here. "Assignee-level" transitions are also permitted to a task's creator and to supervisor/admin (a creator can always do at least what an assignee can); "manager-level" transitions (accept/return/reopen/cancel) require creator or supervisor/admin specifically, never a plain assignee | Matches the spec's fixed state machine exactly; reusing `WorkflowTemplate` would import configurability (custom statuses, reordering) the task spec never asked for | ASSUMED |
| D-168 | Checklist reordering ships with move-up/move-down buttons, not pointer drag-and-drop: fully keyboard-operable (an explicit requirement) without pulling in `@dnd-kit` yet. That dependency is still planned for the "Мой план" Kanban board (a later slice) where drag is the primary interaction, not a secondary one; checklist reordering can adopt it too at that point if wanted, but doesn't need it to satisfy the spec today | Avoid taking on a new frontend dependency before the feature that actually needs pointer drag as its primary interaction exists | ASSUMED |
| D-169 | A subtask's own `POST /tasks` call is rejected (`422` on `parent_task_id`) if the parent itself already has a `parent_task_id` — enforcing "one level of true subtasks" at creation time rather than via a recursive check, since the one-level rule makes "does the parent have a parent" a sufficient test. Creating a subtask also requires `TaskAction.EDIT` on the parent (creator or supervisor/admin), not just `VIEW` — adding a subtask changes the parent's structure (its completion now depends on the subtask), so it needs the same rights as editing the parent directly | Spec: "Prevent self-parenting and cycles"; treating subtask creation as a parent-modifying action is the more defensible permission boundary than allowing any viewer to attach subtasks | ASSUMED |
| D-170 | Bulk actions (`/tasks/bulk/{status,deadline,members,archive,restore}`) do not take a `version` — requiring the client to know every selected task's current version before a bulk call is impractical UX (the list only shows a summary row, not full detail). Each task in a bulk request runs in its own `db.begin_nested()` savepoint via a shared `run_bulk()` helper: a permission or validation failure on one task is recorded in `skipped` (spec: "backend must evaluate permissions for every selected task") without rolling back the others, and the whole batch commits once at the end | Optimistic-concurrency version checks make sense for a single open task a user is actively editing, not a bulk operation over a list row a user hasn't opened; per-task isolation still satisfies the real safety requirement (no partial task left half-updated) | ASSUMED |
| D-171 | `GET /tasks/counters`, `/tasks/deadline-groups` and `/tasks` share one `TaskFilters` dependency and `apply_filters()` function, and a `base_task_query()` helper applies the same scope + `archived_at IS NULL` base to all three — so a filter combination (or a preset built from one) means exactly the same set of tasks in every view. Archived tasks are excluded from all three unconditionally in this slice; there is no "show archived" toggle yet (archived tasks are only reachable via `bulk/restore`, which needs the id, not a search) | Spec requires the List view, Deadline view and counters to agree on what a filter means; consistency was worth centralizing rather than re-deriving the WHERE clause three times | ASSUMED |
| D-172 | Deadline-view week boundaries: "this week" is `[today, Sunday of the current ISO week]` and "next week" is the following Monday–Sunday — a task overdue earlier this week is counted as `overdue`, not `this_week`, so the six date-based groups partition non-completed tasks without overlap (a `completed` task always lands in the `completed` group regardless of its deadline) | The spec names the groups but not their exact boundaries; a non-overlapping partition was the one choice that lets every group's `total` sum to the filtered set's size, which matters for the counters to add up sensibly | ASSUMED |
| D-173 | Configurable List columns (`task_user_preferences.list_columns`, `GET/PUT /tasks/preferences`) cover the columns already in `TaskListItemOut` from slice 2 (status, priority, deadline, creator, assignees, university, created date) — title is always shown, not toggleable. Checklist progress, last activity, tags, contract, originating plan, parent task and overdue duration (also named as optional columns in the spec) are deferred: tags don't exist as a feature yet (no `task_tags` API), and the rest need new batch-loaded fields on the list endpoint. The column *picker* itself (a `<details>` disclosure, keyboard-operable with no extra JS) is built to extend with more entries once those fields exist, so this isn't a dead end, just a smaller first column set | Ship configurability for the fields that already exist rather than block it on building a tags feature and extending the list endpoint's batch queries in the same slice | ASSUMED |
| D-174 | Migration 0011 seeds exactly one permanent template, «Адаптация нового вуза» (14 steps converted 1:1 from the spec's stage list), rather than shipping no seed data or several examples: it doubles as the first real-world exercise of the template schema (dependency chain, one optional step, one approval-required step, checklist items on three steps) and gives a non-empty template list on first use of the editor | The spec's own stage list is already a complete, ordered plan — seeding it directly is more useful than an invented example and cheaper than writing a separate synthetic fixture | ASSUMED |
| D-175 | `interaction_owner` assignee-rule resolution reuses D-159's exact case/whitespace-insensitive fuzzy match of free-text `Launch.owner` against `User.full_name` (0 or >1 matches ⇒ unresolved, surfaced as `assignee_issue` in preview) instead of a second matching heuristic | One matching rule for "free-text owner name → CRM user" across the codebase is easier to reason about and keep consistent than two similar-but-different heuristics | ASSUMED |
| D-176 | `POST /task-plan-templates` creates a template with `steps: []`; steps are added afterward one at a time via `POST .../steps` (by id, not position) rather than requiring the full step list — with dependencies — in one create call | Removes the need for two different step-form variants (position-based for bulk-create vs. id-based for incremental edit) in the frontend; a template with zero steps is a valid, harmless intermediate state | ASSUMED |
| D-177 | `/tasks/templates` is registered as a `hidden: true` entry in `navigation.ts` (reachable by path, absent from the sidebar) rather than a new top-level "Processes"-style nav item, reached only via a supervisor/admin-gated link from the Tasks page and from each university's new "Планы задач" section. `TaskTemplatesPage` itself also gates on `canEditWorkflows`, mirroring `WorkflowsPage`'s forbidden-state pattern, since the route is reachable directly by URL regardless of which links point to it and the backend already restricts template mutations to supervisor/admin | Explicit spec instruction: "Expose the editor through Tasks settings or a secondary Tasks screen. Do not create another top-level Processes item for task plans"; the client-side guard avoids showing edit controls that would only fail server-side on submit | ASSUMED |
| D-178 | `TaskPlanRun.template_snapshot` (JSONB) freezes the template's steps/dependencies/offsets at generation time; later edits to the live template never rewrite already-generated tasks. Progress's "blocked" count is derived at read time from `origin_template_step_key` plus the frozen snapshot's `depends_on_step_id` chain rather than adding a task-to-task dependency column to `tasks` | A generated plan must stay a stable historical record even as the template it came from keeps evolving; deriving "blocked" from data already being stored avoids schema bloat for what is a display-only indicator | ASSUMED |
| D-179 | Plan preview/generate/progress endpoints (`/task-plan-templates/{id}/preview`, `/generate`, `/task-plan-runs*`) are available to any signed-in CRM user (scoped per-request by the existing `check_university_access` — supervisor/admin see everything, others need the university in their managed scope), while template CRUD stays supervisor/admin-only. Starting a plan for a university you manage is normal day-to-day work; editing the reusable template it comes from is configuration | Matches the existing "catalog-style configuration is supervisor/admin, day-to-day use is any assigned manager" boundary already used for workflow templates vs. launches | ASSUMED |
| D-180 | "Мой план" columns are a user-chosen subset/order of the *fixed* task statuses (`new`/`in_progress`/`awaiting_review`/`deferred`/`completed` — `cancelled` excluded, it belongs to the List/Deadline views, not day-to-day planning), not freeform custom columns. Dragging (or explicitly moving) a card between columns is a real status change validated client-side against the same `NEXT_STATUSES` transition table the quick-action buttons already use; an invalid drop is silently ignored rather than sent to the server and rejected | Reusing the existing status model avoids a second, parallel "personal stage" concept with its own rules; the spec's own wording ("личные колонки") fits a personal view/order of the real pipeline rather than an unrelated freeform board | ASSUMED |
| D-181 | `PUT /tasks/preferences` now saves `list_columns`, `planner_columns` and `planner_positions` independently — using Pydantic's `model_fields_set` to update only the fields a given request actually sent — instead of unconditionally overwriting `list_columns` (the only field slice 4 needed). The List view's column picker, the planner's column picker and its drag/reorder positions each call this endpoint on their own, and previously any one of them would have silently wiped the other two | Three independent per-user settings share one small table and one endpoint; a naive full overwrite would make the last-saved picker win and erase the others | ASSUMED |
| D-182 | `TaskListItemOut`/`TaskListItem` (the List view's row shape, also what the planner board fetches) gained a `version` field | Moving a card needs that task's current optimistic-concurrency version to call the existing `/tasks/{id}/status` endpoint (D-160) without an extra per-card detail fetch before every drag | ASSUMED |
| D-183 | The planner board fetches up to 100 of the signed-in user's own tasks (`GET /tasks?scope=mine&limit=100`) in one request and buckets/orders them into columns client-side, rather than adding a dedicated grouped-by-status planner endpoint | A personal board's volume is expected to be small (one person's own open work); a new endpoint would duplicate `apply_filters`/`base_task_query` for no behavioral difference. Revisit only if 100 stops being enough — a fit for slice 7's performance pass, not this one | ASSUMED |
| D-184 | Added `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` (flagged in `docs/design/tasks.md` before this slice, per D-168). Every drag interaction also has an explicit, independently keyboard-operable control built on the exact same handler functions the drag path calls: a per-card "Переместить" status `<select>`, up/down column-order buttons per card, and a column visibility/order settings panel — not reliance on dnd-kit's own keyboard sensor alone (which is also wired via `KeyboardSensor`, as a second path) | Guarantees a fully working, easily testable keyboard/screen-reader path regardless of how well a pointer-drag library's own accessibility layer behaves in practice; matches this codebase's established preference (checklist/template-step reordering, D-168) for explicit buttons as the safety net under a richer interaction | ASSUMED |
| D-185 | The one status transition that requires a comment (`awaiting_review → in_progress`, "Вернуть на доработку") is enforced identically whether triggered from the planner (drag or its select) or the task detail page's quick-action buttons: the `NEEDS_COMMENT`/`TRANSITION_LABELS` constants moved from `TaskStatusActions.tsx` into `api/tasks.ts`, and the small comment-collection form became a shared `StatusCommentModal` component, instead of the planner growing its own copy of this one business rule | One rule, one place — the planner is a second UI for the same status machine, not a separate one; duplicating the comment requirement would risk the two surfaces drifting apart | ASSUMED |
| D-186 | Fixed a pre-existing date-boundary bug in `test_deadline_groups`: it asserted a task due "tomorrow" always lands in the `this_week` group, which is false on the one day the range `(today, week_end]` (D-172) is legitimately empty — when today itself is the week's last day (Sunday). The test now computes `week_bounds(today)` (imported from `task_routes.py`) and only creates/asserts a distinct this-week task when that day isn't Sunday, asserting an empty `this_week` group otherwise | Not a product bug — the partition is correct by construction; the test's fixed `+1 day` offset just happened to collide with the real calendar on the day this slice was finished. Caught because the full suite is run to completion every slice, not skipped when unrelated to the slice's own changes | ASSUMED |
| D-187 | Slice 7 performance pass: `GET /tasks/{id}/comments`, `GET /task-plan-templates` and `GET /task-plan-runs` each queried per-row (comment author + attachments; template steps + checklist items; run's generated tasks + starter) instead of batching, the same N+1 shape `task_list_items()`/`task_out()` already avoided elsewhere. Fixed the same way: `comments_out()`, `templates_out()`, `plan_runs_out()` batch-load with `.in_()` and group in Python; each existing singular helper (`comment_out`, `template_out`) now delegates to the batch version for its one-row call sites | List-viewing paths (opening a task's comments, the template editor, a university's plan runs) are read often; batching them the same way the List/Deadline views already are removes the only remaining per-row query patterns found in the Tasks module | ASSUMED |
| D-188 | Added `test_downgrade_from_head_restores_the_pre_0010_schema`: upgrades to head against a database seeded with real rows (the 0011 seed template, its steps, their checklist items — not empty tables), then downgrades to `0009` and back to `head`, asserting the new tables/columns are gone and the placeholder `owner`/`done` shape is restored. Every other test only ever upgrades — this is the first test to actually execute migrations 0010's and 0011's `downgrade()` functions, which had shipped in slices 1 and 5 but never run. It passed unmodified | Slice 7's explicit "проверка миграции" step; reversible-looking migration code that has never executed is unverified, not verified — a `downgrade()` with a wrong delete order or a missed table only shows up the first time it actually runs | ASSUMED |
| D-189 | `@dnd-kit`'s `DndContext` is given Russian `announcements`/`screenReaderInstructions` (naming the actual card title and column instead of a raw id) instead of its English-only defaults | The app's standing rule to keep all UI text in Russian applies to what a screen reader announces during a drag, not just visible labels; this is the one new interaction pattern in the Tasks module a library's defaults could have silently violated it for | ASSUMED |
| D-190 | `.toolbar` and `.template-step` gained `flex-wrap: wrap` for narrow viewports, closing the two remaining gaps in an otherwise-already-solid responsive layout (sidebar goes off-canvas below 800px, `.table-wrap` scrolls tables horizontally, grids collapse to one column at 1200px, and most Tasks-module rows — counters, bulk-actions bar, filter chips, plan-run rows — already wrapped) | A mobile-responsiveness pass on a module built across six prior slices should confirm and patch, not redesign, a layout system that was already mostly right | ASSUMED |
| D-191 | Declined to code-split the bundle over `@dnd-kit`'s addition (~53 KB raw / ~19 KB gzip; the production build's ">500 KB" note is Vite's default advisory threshold, not a measured problem). No other route in this app is lazy-loaded; adding Suspense boundaries for one feature would be inconsistent with the rest of the app and disproportionate to an unmeasured concern | Revisit only if bundle size becomes an actual, measured complaint (slow load on a real connection), not a build-log advisory | ASSUMED |
| D-192 | Post-baseline UX slice: the List/Deadlines scope tabs show only 5 of the 7 backend `Scope` values (`mine`/`assigned`/`participating`/`team`/`all`) — `created` and `observing` keep full API/policy support (`task_policy.py` untouched) but have no tab. A URL carrying either as `?scope=` (or any other unrecognized value) falls back to `mine` in the frontend only, rather than crashing or rendering no tab as selected | Owner-directed simplification of the visible scope set; hiding a tab must not mean deleting the capability behind it — an old bookmarked/shared link still resolves to something sensible | OWNER |
| D-193 | The Tasks List/Deadlines permanent inline filter panel is replaced by a `TaskFilterButton` (with an active-filter-count badge) that opens a `TaskFilterDialog` — reusing the existing `Modal`/native-`<dialog>` component rather than a second modal implementation. The dialog holds its own draft state (`useState` seeded from the currently-applied URL filters at mount, so it always starts fresh — no reset effect needed); Cancel/Escape/backdrop (all already wired to one `close` prop by `Modal`) discard the draft with no side effects, and only "Сохранить" writes to the URL and to preferences. A compact `TaskFilterSummary` (removable chips, unchanged from the old panel's chip row) sits below the toolbar instead of the panel | Bitrix24-style interaction requested by the owner: an auxiliary control (filtering) was dominating primary page content; explicit draft/cancel/save semantics were an explicit requirement, not just a smaller panel | OWNER |
| D-194 | Saved filter sets live in `task_user_preferences.filters` (new JSONB column, migration 0012) as `{"{view}:{scope}": {...}}`, e.g. `"list:mine"` — one entry per view/scope combination, matching how `planner_columns`/`planner_positions` already extend the same table rather than a new one. `PUT /tasks/preferences` merges the `filters` key into the stored dict (never replaces it wholesale), so saving one scope's filters can never drop another's; each entry is Pydantic-validated (`SavedFilterIn`, `extra="forbid"`, the same `Literal` types `TaskFilters` already uses) — an unknown `{view}:{scope}` key or an invalid status/priority value is a 422, and reads defensively drop any key outside the current known set so a future retired scope/view can't crash old rows | Explicit requirement: schema-validated, migration-safe, backward compatible, restricted to known filter keys and valid values, never an arbitrary unsanitised payload | OWNER |
| D-195 | Switching the view or scope tab clears the six filter-dimension URL params as part of that same navigation (`CLEAR_FILTERS` in `TasksPage.tsx`), rather than only changing `view`/`scope`. Found via the required "no leaking between scopes/views" test: without this, a filter applied to (or restored for) one `{view,scope}` stayed in the URL after switching, which then made the "was an explicit filter already set here?" check for the *new* key see it as explicit and refuse to restore that key's own saved filters | A real bug the spec's own test list (`saved filters do not leak between task scopes` / `between List and Deadlines`) was written to catch — confirms the value of writing those cases before considering the slice done | ASSUMED |
| D-196 | Post-baseline UX slice 2, Deadlines board: added a `later` value to the shared `DeadlinePreset` enum (backend `apply_filters()` and its frontend mirror) so every one of the 6 date-based Deadline-board groups (all but `completed`) has a matching List-view quick filter. Each group's server-truncated tail ("и ещё N…", capped at `group_limit=50` per group since slice 4) is now a real link into the List view pre-filtered to that exact group — `deadline_preset=<group>` for the 6 date buckets, `status=completed` for the completed one — keeping every other currently-applied filter (search, priority, university, scope, ...) instead of just being inert text with no way to see the rest | The board's own truncation note was a dead end — a group with more than 50 tasks had no way to see the other N. Reusing the List view's existing filter machinery (already unified with the board via the filter-dialog slice) needed only one new enum value, not a new endpoint | ASSUMED |
| D-197 | Each Deadline-board group is now a `<details>` disclosure (open by default, same keyboard-operable pattern as the column pickers) instead of a permanently-expanded `<section>`, and each task row now shows priority alongside status — matching the List view's row content, which the board's rows previously omitted | A board someone checks daily benefits from being able to collapse groups they've already triaged; priority was the one List-view row detail missing here for no principled reason | ASSUMED |
| D-198 | The task detail page's section order changed to main info/status → activity feed → checklist → subtasks → comments (activity now second, was last) | Owner-directed reordering; the activity component itself, its event ordering and permissions are untouched — only where the `<TaskActivity>` panel sits among its sibling sections | OWNER |
| D-199 | Structured the filter dialog's Статус/Приоритет fieldsets as bordered "cards" with a 2-column checkbox grid (collapsing to 1 column under 480px) — `.filter-fieldset`/`.filter-checkbox-grid` in styles.css. Checkboxes stay checkboxes (not dropdowns); University/Activity/Checklist/deadline-preset controls are untouched, since the checkbox-and-label-on-one-line and click-label-toggles-checkbox requirements were already true of the existing `.toggle` markup — this was a pure layout change, not a markup/behavior one | Owner-directed layout-only correction; no reason to touch controls the owner didn't ask about | OWNER |
| D-200 | Added `PATCH /tasks/{id}/assignees` (`{assignee_ids: [...]}`, same `TaskAction.REASSIGN` permission and `'reassignment'`/`task.members_update` event types as the existing `PUT /members`) instead of reusing `PUT /members` for inline subtask reassignment. The subtask row only ever has that one subtask's id and its List-item projection (no participants/observers loaded) — sending those as empty arrays through the full endpoint would have silently cleared them. The new endpoint deletes and re-inserts only `TaskMember` rows with `role='assignee'`, leaving other roles' rows untouched | Owner's own suggested shape for exactly this "the full endpoint can't safely update just assignees" situation, confirmed by checking what data the subtask row actually has on hand | OWNER |
| D-201 | The subtask row's inline reassignment control (a `<select>`) is shown only when the current user is the subtask's own creator or `seesAllActions()` (supervisor/admin) — mirroring `TaskAction.REASSIGN`'s server-side rule (`CREATOR_ACTIONS`) exactly, evaluated against the *subtask's* own creator, not the parent task's. Everyone else sees plain text (assignee name or "Без исполнителя"). This is a client-side convenience only — the server is still the enforcement point via the same permission check on `PATCH /assignees` | Matches the owner's explicit "users with reassignment permission see a control, others see read-only text" requirement using the policy rule that already exists rather than inventing a new one | OWNER |
| D-202 | Both boards' layout state lives in four matched `TaskUserPreferences` fields each (`planner_*`/`deadline_*`): `*_columns` (ordered column ids, system codes mixed with `custom:<id>` refs), `*_positions` (manual card order per column), `*_custom_columns` (`{id: {title}}` definitions), `*_custom_members` (`{str(task_id): custom_column_id}` — the only per-task override state; a task absent from it always renders in the system column its real status/deadline maps to). All four are plain replace-on-write, unlike `filters` — the client already holds the full current value from the same `GET /tasks/preferences` that renders the board, so every column add/rename/delete/reorder or card placement sends the complete updated value; no server-side merge needed. Migration 0013 adds the six new columns (`planner_columns`/`planner_positions` already existed) | Extends the existing task-preferences architecture per the owner's explicit instruction, instead of new tables/endpoints, for a concept (personal board layout) the table already models for the planner | OWNER |
| D-203 | A custom column is a pure personal placement override, never a status/deadline change: `boardColumns.ts`'s `bucketTasksByColumn()` checks `customMembers[task.id]` first (if that column still exists) and only falls back to the task's real status/deadline otherwise — shared by both boards via `plannerLogic.ts`'s `bucketByStatus` (now a thin wrapper). Moving a card *out of* a custom column into a *system* column is the one place real data changes: the planner validates it as an ordinary status transition (`isValidMove`/`NEXT_STATUSES`) and, on success, also clears that task's `planner_custom_members` entry so it doesn't stay visually stuck in the column it just left | Spec requirement ("moving a task into one does not change its real status... moving it back into a system column updates its status/deadline"); deriving membership at render time from a small override map avoids a competing source of truth with the task's own fields | OWNER |
| D-204 | Column reordering (system and custom, on both boards) is a pair of move-left/move-right buttons on each column header (`BoardColumnHeader`), not pointer drag-and-drop of the column itself — card movement within/between columns still gets full drag-and-drop (pointer + keyboard, via dnd-kit, already built for the planner). Column create/rename/delete/reorder logic (`boardColumns.ts`, `useBoardColumnActions`) is fully shared between the two boards; only the four preference field names differ | Matches this codebase's established pattern (checklist reorder, template-step reorder, D-168) of explicit buttons as the reliable, easily-keyboard-tested mechanism for reordering a *small* list (a handful of columns), reserving richer drag interaction for the higher-value, higher-cardinality case (many cards) | ASSUMED |
| D-205 | "Сроки" rebuilt as a horizontal Kanban board reusing the exact same infrastructure as "Мой план" (D-202–D-204): `resolveDeadlineColumnOrder()` guarantees all 6 date-based columns are always present (unlike the planner, which lets system columns be hidden) — a saved order is kept as-is except any missing system key is appended at the end. `DeadlinePreset`'s `later` value (D-196) doubles as the exact mapping used both by the column-header "+" button's date prefill and by a drag/select move into that system column: overdue→yesterday, today→today, this_week→end of this week, next_week→end of next week, later→first day after next week, no_deadline→null. Active-only (`active=true`, forced regardless of any saved filter) — the board has no column for completed/cancelled tasks, matching the spec's 6-column list and the prior `deadline-groups` endpoint's own exclusion | Reusing the planner's just-built infrastructure verbatim (only the column-set/labels/classification differ) kept this a wiring exercise rather than a second implementation of column CRUD, card bucketing, drag-and-drop, and custom-column semantics | OWNER |
| D-206 | A card move into a system column reuses the existing `PATCH /tasks/{id}` endpoint (`useMoveTaskDeadline`, just `{deadline, version}`) instead of a new one — no backend change was needed for the whole board. This already gives the move `TaskAction.CHANGE_DEADLINE` permission enforcement, optimistic-concurrency (version conflict → 409), the same generic `'updated'` activity/audit event as any other task edit, and cache invalidation, all for free. On failure, the card's column is derived from query data that's never optimistically updated, so the card is already still in its original spot — "return the card to its previous position" falls out of not doing optimistic UI, rather than needing an explicit revert step | The spec's own words ("use the existing permission checks, optimistic locking, cache invalidation, and activity/audit events") pointed directly at the endpoint the task-detail edit form already uses; a new endpoint would have duplicated all of that | OWNER |
| D-207 | The old server-grouped `TaskDeadlineView`/`useDeadlineGroups`/`GET /tasks/deadline-groups` frontend path is fully replaced (deleted from the frontend) — the new board needs a flat, unbucketed task list to do client-side bucketing/drag/custom-columns, making the server-side 50-per-group cap and its now-pointless "и ещё N…" link (D-196/D-197, itself only one slice old) moot. The backend endpoint, its schema and its own test are left in place (not deleted) — removing a still-working, tested public API endpoint is a more consequential action than trimming an internal frontend hook, and nothing in the request asked for that | Avoid dead frontend code (`deadlineGroupListLink`, `DEADLINE_GROUP_LABELS`, etc. all removed with their tests) without taking the separate, larger step of retiring a public API surface nobody asked to retire | ASSUMED |
| D-208 | Removed the Contract (`Договор`) dropdown from `TaskCreateForm` — the form no longer calls `useContracts`/requests `/contracts`, and no longer sends `contract_id` on `POST /tasks` (the field stays optional server-side, so its absence is a normal partial input, not a `null` write). The interaction (`Взаимодействие`) field, previously paired with Contract in a two-column `.form-row`, now sits alone as a full-width `<label>` so removing Contract doesn't leave an empty grid cell. `TaskEditForm`/`TaskPatch` never had a `contract_id` field to begin with, so editing a task was already structurally incapable of touching an existing contract link — confirmed with a regression test rather than changed. The read-only "Договор" line in the task detail view's `detail-grid` is untouched: it still shows a task's contract when one exists from legacy data, since hiding a still-true historical association would contradict "do not delete historical task-to-contract associations." No backend, migration, endpoint, or Contracts-module change | Owner-directed UI-only removal; `contract_id` remains a valid, independently-settable field on the API for any other caller — this slice only stops the task form from being the one that sets or clears it | OWNER |
| D-209 | The `edu-crm-admin` service account's realm-management permissions are granted live via `scripts/keycloak-grant-admin-permissions.sh` (`kcadm`), not via `deploy/keycloak/realm-edu-crm.json` export data — mirroring `scripts/keycloak-add-public-origin.sh`'s existing live-`kcadm` pattern for changes that only apply to an already-running realm (`--import-realm` skips a realm that already exists, so hand-written service-account role-mapping JSON would never reach a running container anyway, and is fragile to author correctly by hand). Grants exactly `view-users`, `manage-users`, `view-realm` on `realm-management` — explicitly not `manage-realm`, `manage-clients`, or anything broader. Verified live against the real container (not the `KeycloakAdminClient` fake): because `edu-crm-admin` has `fullScopeAllowed: false` (by design — it must not implicitly receive every realm/client role), granting the roles to the service-account *user* alone (`add-roles`) was not sufficient — a `client_credentials` token came back with no `realm_access`/`resource_access` claims at all, and every admin-API call 403'd, until the same three roles were *also* added to the `edu-crm-admin` client's own `scope-mappings` (`clients/{id}/scope-mappings/clients/{realm-management-id}`). The script now does both grants; a manual `client_credentials` token exchange followed by `GET /admin/realms/edu-crm/users` and `GET /admin/realms/edu-crm` confirmed real HTTP 200s and a populated user list / password policy | Least-privilege client configuration (`fullScopeAllowed: false`) has a real, non-obvious consequence — user-role assignment and client-scope-mapping are two separate grants in Keycloak, and only testing against the fake (as Tasks 1–2 did) could never have surfaced this; end-to-end verification against the live container was the point of this task | ASSUMED |
| D-210 | `app/email.py`'s HTTP-provider request/response shape (`POST` with a JSON `{"to", "from", "subject", "text"}` body, `Authorization: Bearer <key>` header, any non-2xx or network failure raises `EmailSendError`) is an invented, plausible generic contract — no real email provider's documented API was available in this environment, exactly like `app/sms.py`'s own disclaimer (D-157) for the SMS gateway. | Why: matches the established SMS precedent for "no real provider available → build the injectable seam and a logging-only default, document that the concrete HTTP shape is a placeholder for whoever wires up a real provider." | ASSUMED |
| D-211 | The email sender-identity catalog (`EmailSenderIdentity`, `/api/v1/email-senders`) is entirely admin-managed (create/deactivate gated to supervisor/admin via `sender_manager`) with no self-service verification flow (no "click a link in this email to prove you own it" step). | Why: an admin adding an address to the catalog already vouches for it being a legitimate university sending address; a verification-link flow would need its own token/expiry/click-through machinery for a case (a small number of admin-curated institutional addresses) where the added complexity buys nothing. | ASSUMED |
| D-212 | `POST /api/v1/email-senders/test` always sends to the caller's own Keycloak-sourced `auth.user.email`, never a client-supplied address, and is rate-limited to once per 60 seconds per user (`email_test_sent_at` on `users`, migration 0016) — mirroring `profile_routes.py`'s SMS-code cooldown (D-156/D-157). | Why: an endpoint that could send to an arbitrary address would be an open mail-relay-testing primitive regardless of auth; the cooldown keeps it from being usable to spam a provider or a mailbox. | ASSUMED |
| D-213 | Deactivating a sender identity (`DELETE /api/v1/email-senders/{id}`) is a soft delete (`is_active = False`); recreating the same address later (`POST` with the same `email_address`) reactivates the existing row in place (same `id`, refreshed `display_name`/ `created_by_user_id`) instead of erroring or creating a duplicate. | Why: an address a university stops using today may need to come back later (a returning staff member's address, a temporarily-paused catalog entry); hard delete would also orphan any historical `email_sender.create`/`.deactivate` audit events and any user rows still pointing at the deleted id via `email_sender_identity_id`. | ASSUMED |
| D-214 | `GET/POST /api/v1/admin/*` (`admin_routes.py`) is a new, `crm-superadmin`-only surface backed by `app.state.keycloak_admin` (a `KeycloakAdminClient` now constructed unconditionally in `create_app()`, alongside `oidc`/`sms_sender`/`email_sender` — empty credentials just make `is_configured()` False, per its existing contract from D-209/slice 2). `GET /admin/users` reads the CRM's own `users` table (count + login/email/roles/status); `GET /admin/pending-registrations` reads Keycloak directly via `KeycloakAdminClient.list_users()`, filtered to accounts with a real email and none of `oidc.CRM_ROLES` — a "pending registration" is exactly a Keycloak account that can sign in but has no CRM role yet, so the CRM never created a local `users` row for it. | Why: this is the "pending registrations = Keycloak users without a CRM role" model the owner specified for the simple-RBAC design (see the architecture clarification in the Настройки project); reusing `list_users()`/`assign_realm_role()` from the already-reviewed slice-2 admin client avoids a second Keycloak integration path. | OWNER |
| D-215 | `POST /admin/pending-registrations/{id}/approve` only ever grants the single baseline `crm-user` role and has no corresponding "reject" endpoint — leaving an account unapproved already has the same effect (no CRM access) as rejecting it, and Keycloak itself is where an account would be disabled outright if that were ever needed. `list_users()`'s default page size (200) is not paginated further here — acceptable for this realm's current size; revisit if it stops being enough. | Why: matches the owner's explicit "no complicated permission matrix, one superadmin, everyone else is a plain crm-user unless promoted later by hand" instruction exactly — a promote-to-supervisor/admin/superadmin action is a separate, not-yet-built concern, not part of first-time approval. | OWNER |
| D-216 | Design tokens: every colour, font size, weight, radius, spacing step and shadow in `frontend/src/styles.css` is a CSS custom property in the `:root` block (`docs/design/tokens.md`); component rules reference tokens only, enforced by `src/styles.test.ts`. The ~190 near-duplicate hard-coded colours were consolidated onto one violet-tinted neutral scale, one accent, four status hues, a chart set and a sidebar set. Minimum text size is 11px (was 7–9px in 76 places), base size 14px, and every text token meets WCAG AA (≥ 4.5:1) on the surfaces it is used on | Owner asked for a design overhaul; a token layer first makes the page redesigns that follow (and a later dark theme) a matter of values, not a hunt through 3,000 lines of literals | OWNER |
| D-217 | Задачи «Список» redesigned CRM-style (Bitrix24 / Asana): one toolbar row (scope dropdown, search field with the applied filters as removable chips inside it, a small «Список/Сроки/Мой план» segmented switcher, «Создать задачу»), then one row of quick-filter counter chips; the two stacked tab rows, the sort `<select>` and the separate chip row are gone. Sorting is by clicking column headers (`aria-sort`). The filter dialog keeps its draft/«Сохранить» semantics (D-192–D-195) but now offers «Исполнитель» and «Постановщик» (API filters that already existed) instead of «Чек-лист» and «Активность» (the latter duplicates status and the «Открытые» chip); old presets carrying `active`/`has_checklist` still load and show as removable chips. Rows show institution · interaction and checklist/subtask/comment counts under the title, a relative, colour-coded deadline, assignee avatars, a priority mark and «Изменена»; a quick-add line creates a task assigned to the current user. `TaskListItemOut` gained `interaction`, `checklist_progress`, `subtasks`, `comment_count`, `updated_at` (batch-loaded, fixed query count per page); `SavedFilterIn` gained `assignee_id`/`creator_id`; `sort=priority` now orders by severity (it was alphabetical) and `sort=updated_at` is accepted. The sidebar «Задачи» badge now shows open tasks (was the total) and turns red when any are overdue | Owner: "more basic information, fewer unnecessary filters, similar to top CRMs". No migration — all new list fields are computed | OWNER |
| D-218 | Task detail page is two columns: title (inline rename — pencil, Enter/blur saves, Escape cancels), status actions with the forward move as the one primary button (new→Начать, in_progress→Завершить or «Отправить на проверку» when review is required, awaiting_review→Принять, deferred→Возобновить), inline description, then history → checklist → subtasks → comments (D-198 order kept) on the left; a sticky property sidebar on the right where deadline, planned start, priority, «Требует проверки» and assignees are edited in place (PATCH /tasks/{id}, PATCH /tasks/{id}/assignees). The separate «Изменить» edit form is gone. A successful inline save writes the returned task into the query cache immediately, so back-to-back edits carry the new version instead of hitting the optimistic-lock 409. Under 1100px the sidebar sits between the title block and the rest. Participants/observers stay read-only here (the members endpoint replaces all three lists; a picker for them is future work) | Owner: design overhaul, "similar to top CRMs"; also fixes the fields that sat flush against the panel edge | OWNER |
| D-219 | «Сроки» and «Мой план» share one compact card (`BoardCard.tsx`): title, deadline as the same colour-coded relative pill as the list, a priority mark only for high/urgent, the status badge only on «Сроки» (on «Мой план» the column is the status), institution · interaction, assignee avatars and checklist/comment counts. The keyboard/screen-reader alternative to dragging (↑/↓ within the column, the «Переместить» select) moves behind a "⋯" `<details>` disclosure on each card instead of filling every card; labels and behaviour are unchanged (D-180, D-204) | The explicit controls took about half of each card's height on both boards. A native disclosure keeps them one Enter/Space away for keyboard users with no extra JS | OWNER |

## Proposals awaiting owner approval

| ID | Proposal | Why it is not assumed |
|---|---|---|
| P-001 | MinIO for attachments | New external service (D-010) |
| P-002 | ClamAV scanning of uploads | New external service; about 1–3 GB RAM |
| P-003 | Branch protection on `main` | Repository setting; owner action |

## Flagged follow-up work (handover, 2026-09-22)

Not decisions — open items carried forward for whoever picks up this branch next. Full detail
and evidence in `report file/2026-09-22-handover-report.md`.

| ID | Item | Priority | Status on `ai/phone-verification` |
|---|---|---|---|
| F-001 | Rostelecom LMS + Laravel CMS website API integration (spec's "Data sources" section; D-008 already authorizes building this as a labelled mock until the real contract arrives). A complete, tested, documented mock connector (`app/connectors.py`/`app/connector_routes.py`, `docs/api/integrations.md`, migration `0013_integration_links`, D-184–D-187) **already exists**, but only on the unreconciled sibling branch `ai/integration-candidate` (`.worktrees/ai-integration-candidate`) — it has never been merged or ported into this branch, which currently has zero integration code (no connector module, no `IntegrationLink` model, no route). | **IMPORTANT** | NOT STARTED here (built elsewhere, unmerged) |
| F-002 | General interface-design polish pass needed — visual density and spacing/margin consistency across pages. Specifically re-verify the Задачи page after its 2026-09-22 redesign (tab sizing, the counters/«Шаблоны планов» utility row) on narrow and wide viewports; check other pages for the same kind of crowding this pass just fixed there. | Design | IN PROGRESS on `ai/design-tokens`: tokens + type scale (D-216), the Задачи list (D-217), the task detail page (D-218) and the board cards (D-219) done; the other pages remain |
